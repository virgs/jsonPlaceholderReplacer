language: node_js

node_js:
    - 12
    - 14

before_install:
    - npm install

cache:
    directories:
        - node_modules

script:
    - npm run build
    - npm test
    - npm run lint
    - npm run codeCoverage
